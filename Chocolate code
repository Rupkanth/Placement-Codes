t=int(input())
for i in range(t):
  n=int(input))
  arr=list(map(int,input.split()))
  a=[]
  b=[]
  for i in range(n):
     if i==0 or if i%2==0:
        a.append(arr[i])
     else:
        b.appand(arr[i])
  ans=max(sum(a),sum(b))
  print(ans)
